const initialState = {
  userAuthResponse: "",
};

const UserAuthReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default UserAuthReducer;
